# BOOKORA AI Agent Portable

Portable Windows desktop shell for BOOKORA AI.

- Run the portable EXE.
- Optionally pass a web URL as the first argument.
- No Supabase secrets are packaged into the desktop app.
- Production URL is supplied at build time through BOOKORA_WEB_URL.
- Local development defaults to http://localhost:8080.
